import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.tu_nombre.calculadoraapu',
    appName: 'Calculadora APU',
    webDir: 'dist'
};

export default config;
